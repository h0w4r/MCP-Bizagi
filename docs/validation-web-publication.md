# Native Web execution baseline — 2026-09-08 UTC

This record describes source after the immutable `0.6.0-alpha.1` release.
Installed engine: **Bizagi Modeler 4.3.0.008**. It is not a full Modeler automation
claim or an accreditation of every browser and document combination.

## Actual MCP/native circuit

Run `20260908-152017-5598b8` completed through the independent stdio MCP client:

- **10 terminal operations: 8 completed, 1 expected failure, 1 cancellation.**
- Two-diagram native creation, nested structural edits and two native
  extended-attribute/embedded-file transactions.
- Selected-root publication with its embedded subprocess, all-diagram publication,
  and a successful publication after active-registration cancellation.
- Missing selected GUID failed in the actual native workflow. Duplicate selection
  was separately rejected at the MCP preflight boundary.
- Unicode title/names/descriptions, exact selected page/search inventories,
  parent references, native PNG hashes and embedded attachment SHA-256 passed
  fresh-worker readback. Unselected task/description sentinels were absent from
  selected output. Final native inspection confirmed the original revision.
- **18 worker records and 41 owned-process records**, all exited. Periodic
  read-only observations recorded no visible owned window or owned foreground
  process. Sampling is not a continuous desktop-observation guarantee.

Selected publication operation: `6c2f824294dd44edb9eb182a0f1a10e0`.
Recovery publication: `b8c6fb5a52a14f3fbd3c5ce42831bc1e`.

Private receipt hashes:

| Record | SHA-256 |
| --- | --- |
| MCP transcript | `c39ffbab89e65c36622307cda5b66d8391bfad07648c2fdcebea8a684822fd56` |
| Native publication receipts | `3a78ffbdea8d3e7b3181ba4518609a1bbf942834d1a8b39e490f0886b0172c88` |

## Regression and browser boundaries

Locked restore and the Release solution build passed with zero warnings/errors;
**1,080 unit/component tests** passed without skips. These isolate reader and
comparison policies and do not accredit the native engine. Real XML MCP run
`20260908-154554-8780c6` also passed; it explicitly records native execution as
not run rather than treating discovery as native success.

Run `20260908-153236-585086` passed actual Excel, Word and PDF publication
against the previously used nested native corpus. Each used an independent
reader and retained its source bytes. This specifically covers the change from
basename-only image-folder matching to the exact operation render directory;
native Web viewer icon folders must not count as native diagram renders.

The actual selected site was served only on loopback for browser inspection.
Desktop navigation, nested-task search leading to its subprocess, loaded native
images, parent navigation and the root task's description/attachment link were
observed. The private frontend-quality journey separately exercises reload,
attachment HTTP bytes and failure/recovery on a nonexistent local artifact.
The desktop functional journey completed, including reload, the exact HTTP
attachment hash `c3b288b545076f681cb415f5f565a0848819512a69d4094acf88e2dc45be01a5`
and recovery after an actual 404. The subsequent automatic accessibility step
reported `aria-roles`, `color-contrast` and `link-name` violations in the installed
viewer. The mobile journey did not complete: its responsive navigation/layout
differs from desktop and the current assertions did not establish the intended
route. No baseline was approved and no vendor assets were patched to hide these
results. The broader frontend-quality gate remains **unaccredited**, not
implicitly green because native file readback passed. Raw sites/screenshots can identify the local
operator and are not published in this repository.

## Retained diagnostics, not successful acceptance

- A guessed logo-enum namespace failed native type loading; the adapter now uses
  the actual property's enum type.
- Native Web generation succeeded but the original host render inventory also
  counted viewer jQuery icon folders named `images`; exact directory matching
  corrected that false comparison without weakening PNG identity checks.
- Browser inspection exposed missing nested search containers. The installed
  native mapper now supplies the selected surface entries. Its DTOs must be
  serialized with the installed JSON assembly so native property annotations are
  honored; a mismatched serializer was rejected by independent readback.
- The authored embedded-attribute test initially used noncanonical XML child
  order. Correcting the input to native serialization order retained the strict
  comparison rather than accepting arbitrary differences.
- Excel publication of the newer two-diagram Web corpus failed because the
  current expected-name gate includes the implicit participant name that the
  native workbook omits (`20260908-153042-7af9b9`). This remains a distinct
  diagnostic/coverage gap; the successful older-corpus regression does not erase
  it or accredit all Excel inputs.

The native [publication contract](native-web-publication.md) takes precedence over
broader interpretations of build status or the existence of a tool declaration.
